{
  "name": "controle-financeiro-api",
  "version": "1.0.0",
  "description": "Aplicação de controle financeiro desktop com Electron, Node.js e SQLite.",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "package": "electron-packager . ControleFinanceiro --platform=win32 --arch=x64 --out=dist --overwrite",
    "package-mac": "electron-packager . ControleFinanceiro --platform=darwin --arch=x64 --out=dist --overwrite",
    "package-linux": "electron-packager . ControleFinanceiro --platform=linux --arch=x64 --out=dist --overwrite",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Seu Nome <seu.email@example.com>",
  "license": "MIT",
  "dependencies": {
    "electron": "^28.0.0",
    "sqlite3": "^5.0.0"
  },
  "devDependencies": {
    "electron-packager": "^17.0.0"
  }
}